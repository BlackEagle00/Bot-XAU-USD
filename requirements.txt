# Dependencias — comunes a los 4 bots (Oro/EUR · swing/scalping).
# Instalar con:  pip install -r requirements.txt
# (Cada carpeta de bot mantiene una copia idéntica para poder instalar también
#  desde dentro de su carpeta; este es el archivo canónico de la raíz.)

MetaTrader5>=5.0.37      # API oficial de MT5 para Python (solo Windows)
pandas>=1.5.0            # Manejo de DataFrames OHLCV
numpy>=1.24.0            # Cálculos numéricos de indicadores
python-dotenv>=1.0.0     # Carga de variables de entorno desde .env (seguridad)
